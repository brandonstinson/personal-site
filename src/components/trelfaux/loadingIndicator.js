import React from 'react';
import Loader from 'react-loader-spinner';

const LoadingIndicator = () => <Loader type="Triangle" color="#000000" width={80} height={80} />;

export default LoadingIndicator;
