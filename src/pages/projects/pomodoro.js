import React from 'react';

import Layout from '../../components/Layout';
import Header from '../../components/Header';

const PomodoroPage = () => (
    <Layout>
        <Header />
        <h1>This is where my pomodoro clock will go...</h1>
    </Layout>
);

export default PomodoroPage;
